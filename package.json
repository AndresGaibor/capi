{
  "name": "capi",
  "module": "src/cli.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jsdom": "^28.0.3",
    "jsdom": "^29.1.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "citty": "^0.2.2",
    "consola": "^3.4.2"
  },
  "scripts": {
    "capi": "bun run src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun test",
    "smoke:qwen": "bun run scripts/smoke-qwen.ts",
    "smoke:deepseek": "bun run src/cli.ts chat enviar --proveedor deepseek --modelo default \"Responde solamente con la palabra OK\"",
    "coverage": "rm -rf coverage && bun test --coverage --coverage-reporter=lcov && bun run scripts/verificar-cobertura.ts",
    "verify": "bun run typecheck && bun test && bun run coverage"
  }
}
