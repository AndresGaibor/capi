import{expect,test}from"bun:test";import{EscritorEditorWeb}from"../../src/proveedores/compartido/EscritorEditorWeb";
test("degrada de fill a setter nativo y confirma valor",async()=>{const acciones:string[]=[];const t:any={rellenar:async()=>{acciones.push("fill");throw new Error("Uncaught")},evaluar:async(code:string)=>{acciones.push(code.includes("HTMLTextAreaElement")?"native":"confirm");return{value:code.includes("return {ok")?{ok:true,tipo:"textarea"}:{coincide:true}}}};const r=await new EscritorEditorWeb(t).escribir("textarea","hola");expect(r.estrategia).toBe("setter-nativo");expect(acciones[0]).toBe("fill");});
test("usa CDP cuando DOM no confirma",async()=>{const metodos:string[]=[];const t:any={evaluar:async(code:string)=>({value:code.includes("return {ok")?{ok:false}:{coincide:false}}),cdp:async(m:string)=>{metodos.push(m)}};const r=await new EscritorEditorWeb(t).escribir("textarea","hola");expect(r.estrategia).toBe("cdp");expect(metodos).toContain("Input.insertText");});
