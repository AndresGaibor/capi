export function scriptEnviarPromptQwen(prompt: string): string {
  return `(async()=>{const ta=document.querySelector('textarea.message-input-textarea');if(!ta)return{ok:false,error:'Textarea no encontrado'};const setter=Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value').set;setter.call(ta,${JSON.stringify(prompt)});ta.dispatchEvent(new Event('input',{bubbles:true}));ta.dispatchEvent(new Event('change',{bubbles:true}));for(let i=0;i<100;i++){const btn=document.querySelector('button.send-button[aria-label="Enviar"],button.send-button,.chat-prompt-send-button button');if(btn&&!btn.disabled){btn.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true}));btn.dispatchEvent(new MouseEvent('mousedown',{bubbles:true}));btn.click();for(let j=0;j<50;j++){const t=document.querySelector('textarea.message-input-textarea');if(!t||t.value.trim()===''||location.pathname.startsWith('/c/'))return{ok:true};await new Promise(r=>setTimeout(r,100));}return{ok:false,error:'Qwen no inició el envío'};}await new Promise(r=>setTimeout(r,100));}return{ok:false,error:'Botón de envío no disponible'};})()`;
}
