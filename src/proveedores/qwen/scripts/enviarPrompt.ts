import { SELECTORES_QWEN } from "../selectores/SelectoresQwen";
export function scriptEnviarPromptQwen(prompt: string): string {
  return `(async()=>{const ta=document.querySelector(${JSON.stringify(SELECTORES_QWEN.textarea)});if(!ta)return{ok:false,error:'Textarea no encontrado'};const setter=Object.getOwnPropertyDescriptor(HTMLTextAreaElement.prototype,'value').set;setter.call(ta,${JSON.stringify(prompt)});ta.dispatchEvent(new Event('input',{bubbles:true}));ta.dispatchEvent(new Event('change',{bubbles:true}));for(let i=0;i<80;i++){const btn=document.querySelector(${JSON.stringify(SELECTORES_QWEN.enviar)});if(btn&&!btn.disabled){btn.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true}));btn.dispatchEvent(new MouseEvent('mousedown',{bubbles:true}));btn.click();for(let j=0;j<30;j++){const t=document.querySelector(${JSON.stringify(SELECTORES_QWEN.textarea)});if(!t||t.value.trim()===''||location.pathname.startsWith('/c/'))return{ok:true};await new Promise(r=>setTimeout(r,100));}return{ok:false,error:'Qwen no inició el envío'};}await new Promise(r=>setTimeout(r,100));}return{ok:false,error:'Botón de envío no disponible'};})()`;
}
