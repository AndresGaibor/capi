import { defineCommand } from "citty";import { crearAplicacion } from "../../composicion/crearAplicacion";
export const comandoEstadoImportar=defineCommand({meta:{name:"importar",description:"Importar y fusionar un estado portable"},args:{archivo:{type:"string",required:true},confirmar:{type:"boolean",default:false}},run:async({args})=>{if(!args.confirmar)throw new Error("Usa --confirmar para importar");const data=JSON.parse(await Bun.file(String(args.archivo)).text());const r=crearAplicacion().gestionarEstadoProyecto.importar(data);console.log(JSON.stringify({ok:true,...r}));}});
