import { defineCommand } from 'citty';
import { diagnosticarEjecucion } from '../tareas/durable';
import { TransporteWebBridge } from '../../../../plataforma/webbridge/TransporteWebBridge';
import { LectorTelemetriaQwen } from '../../../../proveedores/qwen/navegador/LectorTelemetriaQwen';
export const comandoDiagnosticoEjecucion=defineCommand({meta:{name:'ejecucion',description:'Diagnosticar una ejecución durable'},args:{id:{type:'positional' as const,required:true},output:{type:'string' as const,default:'json'}},async run({args}){const d=diagnosticarEjecucion(String(args.id));if(!d){process.stderr.write(`Ejecución no encontrada: ${String(args.id)}\n`);process.exitCode=1;return}let telemetria:unknown={disponible:false,motivo:'no_aplica'};if(d.proveedor==='qwen'){const transporte=new TransporteWebBridge();telemetria=await new LectorTelemetriaQwen(transporte).leer(d.conversacionId)}process.stdout.write(JSON.stringify({...d,telemetriaQwen:telemetria},null,args.output==='human'?2:0)+'\n')}});
